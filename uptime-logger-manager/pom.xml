<project xmlns="http://maven.apache.org/POM/4.0.0"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <!-- parrent pom -->
    <parent>
        <groupId>${app.parent.groupId}</groupId>
        <artifactId>${app.parent.artifactId}</artifactId>
        <version>${app.parent.version}</version>

        <relativePath>../pom.xml</relativePath>
    </parent>
    <!-- ════════════════════════════════════ -->

    <!-- inherited -->
    <!-- <groupId>${app.parent.groupId}</groupId>
    <version>${app.parent.version}</version>   
    -->
    <!-- current -->
    <packaging>${app.child.packaging}</packaging>
    <artifactId>${app.artifactId}</artifactId>
    <name>${app.name}</name>
    <description>${app.description}</description>
    <properties>
        <app.artifactId>uptime-logger-manager</app.artifactId>
        <app.name>uptime logger manager</app.name>
        <app.description>Installs and manages the state of uptime logger service.</app.description>
        <app.entryPoint>EntryPointULM</app.entryPoint>
    </properties>
    <dependencies>
        <!-- https://mvnrepository.com/artifact/org.openjfx/javafx-graphics -->
        <!-- <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-graphics</artifactId>
            <version>22</version>
        </dependency> -->
        <!-- https://mvnrepository.com/artifact/org.openjfx/javafx-controls -->
        <!-- <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-controls</artifactId>
            <version>22</version>
        </dependency> -->
        <!-- https://mvnrepository.com/artifact/org.openjfx/javafx-media -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-media</artifactId>
            <version>22</version>
        </dependency>
        <!-- https://mvnrepository.com/artifact/org.openjfx/javafx-fxml -->
        <dependency>
            <groupId>org.openjfx</groupId>
            <artifactId>javafx-fxml</artifactId>
            <version>22</version>
        </dependency>
        <dependency>
            <groupId>${app.parent.groupId}</groupId>
            <artifactId>${app.uptimeServiceName}</artifactId>
            <version>${app.parent.version}</version>
        </dependency>
    </dependencies>
    <build>
        <finalName>${project.artifactId}</finalName>
        <plugins>
            <plugin>
                <!-- https://mvnrepository.com/artifact/org.codehaus.mojo/properties-maven-plugin -->
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>properties-maven-plugin</artifactId>
                <version>1.2.1</version>
                <executions>
                    <execution>
                        <phase>generate-resources</phase>
                        <goals>
                            <goal>write-project-properties</goal>
                        </goals>
                        <configuration>
                            <outputFile>${project.build.outputDirectory}/app.properties</outputFile>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.5.1</version>
                <configuration>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-dependency-plugin</artifactId>
                <version>3.1.2</version>
                <executions>
                    <execution>
                        <id>copy-uptime-loger-service-jar</id>
                        <phase>process-resources</phase> <!-- Copy during resource processing -->
                        <goals>
                            <goal>copy</goal>
                        </goals>
                        <configuration>
                            <artifactItems>
                                <artifactItem>
                                    <groupId>${app.parent.groupId}</groupId>
                                    <artifactId>${app.uptimeServiceName}</artifactId>
                                    <version>${app.parent.version}</version>
                                    <outputDirectory>${basedir}/src/main/resources/services</outputDirectory>
                                    <destFileName>${app.uptimeServiceName}.jar</destFileName>
                                </artifactItem>
                            </artifactItems>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>2.1</version>
                <executions>
                    <execution>
                        <phase>package</phase>
                        <goals>
                            <goal>shade</goal>
                        </goals>
                        <configuration>
                            <transformers>
                                <transformer
                                    implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                    <mainClass>be.freeaime.app.base.${app.entryPoint}</mainClass>
                                </transformer>
                            </transformers>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>